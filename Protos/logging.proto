syntax = "proto3";

option csharp_namespace = "RaspRun.Grpc";

service Logger {
  rpc WatchLogs(LogRequest) returns (stream LogMessage);
}

message LogRequest {
  string filter = 1;
}

message LogMessage {
  string level = 1;
  string message = 2;
  string timestamp = 3;
  string source = 4;
}
